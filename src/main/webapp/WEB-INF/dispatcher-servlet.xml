<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/data/jpa
        http://www.springframework.org/schema/data/jpa/spring-jpa.xsd">

    <context:component-scan base-package="com.tas"/>

    <!-- ViewResolver -->
    <bean id="templateResolver" class="org.thymeleaf.templateresolver.ClassLoaderTemplateResolver">
        <property name="prefix" value="/views/"/>
        <property name="suffix" value=".html"/>
        <property name="templateMode" value="HTML5"/>
        <property name="characterEncoding" value="UTF-8"/>
    </bean>

    <bean id="templateEngine" class="org.thymeleaf.spring4.SpringTemplateEngine">
        <property name="templateResolver" ref="templateResolver"/>
    </bean>

    <bean class="org.thymeleaf.spring4.view.ThymeleafViewResolver">
        <property name="characterEncoding" value="UTF-8"/>
        <property name="templateEngine" ref="templateEngine"/>
    </bean>


    <mvc:resources mapping="/dist/css/**" location="classpath:/static/dist/css/"/>
    <mvc:resources mapping="/dist/img/**" location="classpath:/static/dist/img/"/>
    <mvc:resources mapping="/dist/js/**" location="classpath:/static/dist/js/"/>

    <mvc:resources mapping="/plugins/fontawesome-free/css/**"
                   location="classpath:/static/plugins/fontawesome-free/css/"/>
    <mvc:resources mapping="/plugins/fontawesome-free/webfonts/**"
                   location="classpath:/static/plugins/fontawesome-free/webfonts/"/>
    <mvc:resources mapping="/plugins/jquery/**"
                   location="classpath:/static/plugins/jquery/"/>

    <!-- Load Annotation @Valid,@ControllerAdive... -->
    <mvc:annotation-driven/>

    <!-- Load Bean Respository -->
    <jpa:repositories base-package="com.tas.repository"/>

    <!-- For reading the properties files -->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="/WEB-INF/i18n/messages"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!-- Locale Change Interceptor to achieve internationalization. -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
                <property name="paramName" value="lang"></property>
            </bean>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- Cookie Resolver -->
    <bean id="localeResolver" class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
        <property name="cookieMaxAge" value="360000"/>
    </bean>

    <!-- For use Validator I18N -->
    <bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
        <property name="validationMessageSource" ref="messageSource"/>
    </bean>

    <!-- Load validator in I18N Spring mvc -->
    <mvc:annotation-driven validator="validator"/>

    <bean id="model-mapper" class="org.modelmapper.ModelMapper">
    </bean>

</beans>
