package com.tas.common.scheduling.timer;

import org.quartz.Job;

import java.util.*;
import java.util.concurrent.CountDownLatch;
import java.util.stream.Collectors;

public class TestTimer {
    static int cout=0;
    public static void main(String[] args) {
        Timer timer=new Timer();

        int count=0;


//        List<Integer> list=new ArrayList();
//        for (int i = 0; i <50 ; i++) {
//            list.add(i);
//        }

//        list.stream().skip(3).limit(10).forEach(integer -> System.out.println(integer));
//        System.out.println();
////        list.stream().sorted((a,b) -> b-a).forEach(integer -> System.out.println(integer));
//        List<Integer> list1=list.stream().skip(3).limit(10).collect(Collectors.toList());
//        list.forEach(System.out::println);

    }

}
